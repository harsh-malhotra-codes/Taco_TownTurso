// Admin Dashboard JavaScript - Clean and Simple
class AdminDashboard {
    constructor() {
        this.init();
    }

    init() {
        console.log('Admin Dashboard initialized');
        // This file is now integrated into admin.html
        // All functionality is handled directly in the HTML file
    }
}

// Initialize when page loads
document.addEventListener('DOMContentLoaded', () => {
    new AdminDashboard();
});
